'use client';import { useState, useEffect } from 'react';import Link from 'next/link';import PersonCard from '@/components/PersonCard';import AnimatedSection from '@/components/AnimatedSection';import { PersonWithStats } from '@/types';import { Users, Search } from 'lucide-react';import { useLang } from '@/components/LanguageProvider';
export default function TeachersPage() { const { t } = useLang(); const [tt, setTt] = useState<PersonWithStats[]>([]); const [s, setS] = useState(''); const [l, setL] = useState(true); useEffect(()=>{fetch('/api/people?type=teacher').then(r=>r.json()).then(d=>{if(Array.isArray(d))setTt(d);setL(false)}).catch(()=>setL(false))},[]); const ft = s.trim()?tt.filter(p=>p.name.toLowerCase().includes(s.toLowerCase())):tt;
return (<div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-24"><AnimatedSection><div className="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-8"><div><div className="flex items-center gap-2 text-sm text-neutral-400 mb-1"><Users size={16}/><span>{t('teachers.title')}</span></div><h1 className="text-3xl font-bold text-neutral-900 tracking-tight">{t('teachers.heading')}</h1><p className="text-sm text-neutral-500 mt-1">{t('teachers.desc')}</p></div><div className="relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-neutral-400"/><input type="text" placeholder={t('teachers.search')} value={s} onChange={e=>setS(e.target.value)} className="w-full sm:w-56 pl-9 pr-4 py-2.5 text-sm glass-card rounded-2xl focus:outline-none focus:ring-2 focus:ring-black/[0.06] transition-all placeholder:text-neutral-300"/></div></div></AnimatedSection>{l?(<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3 sm:gap-4">{Array.from({length:4}).map((_,i)=>(<div key={i} className="glass-card rounded-2xl aspect-[3/4]"><div className="h-full animate-shimmer rounded-2xl"/></div>))}</div>):ft.length>0?(<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3 sm:gap-4">{ft.map((p,i)=>(<PersonCard key={p.id} person={p} index={i}/>))}</div>):(<div className="text-center py-16"><Users size={40} className="mx-auto text-neutral-300 mb-3"/><p className="text-neutral-500 text-sm">{t('teachers.empty')}</p><Link href="/register" className="text-sm text-neutral-600 hover:text-neutral-900 mt-2 inline-block font-medium">{t('teachers.first')}</Link></div>)}</div>);
}
