'use client';import { useState } from 'react';import { motion } from 'framer-motion';import { MessageSquare, Check, Send } from 'lucide-react';import AnimatedSection from '@/components/AnimatedSection';import { useLang } from '@/components/LanguageProvider';
export default function SuggestPage() { const { t } = useLang(); const [c, setC] = useState(''); const [ct, setCt] = useState(''); const [e, setE] = useState(''); const [ok, setOk] = useState(false); const [l, setL] = useState(false); const hs = async () => { setE(''); if(!c.trim()){setE('请输入建议内容');return;} setL(true); try { const r = await fetch('/api/suggestions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content:c.trim(),contact:ct.trim()})}); if(r.ok){setOk(true);setC('');setCt('');} else { const d = await r.json(); setE(d.error||'提交失败'); } } catch { setE('网络错误'); } setL(false); };
return (<div className="max-w-lg mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-24"><AnimatedSection><div className="text-center mb-8"><div className="w-12 h-12 rounded-xl bg-green-50 flex items-center justify-center mx-auto mb-3"><MessageSquare size={24} className="text-green-600"/></div><h1 className="text-2xl font-bold text-neutral-900 tracking-tight">{t('suggest.title')}</h1><p className="text-sm text-neutral-500 mt-1">{t('suggest.desc')}</p></div></AnimatedSection><AnimatedSection delay={0.1}><div className="bg-white rounded-2xl border border-neutral-200/60 p-5 sm:p-6 space-y-4">{ok?(<motion.div initial={{scale:0.9,opacity:0}} animate={{scale:1,opacity:1}} className="text-center py-8"><Check size={48} className="mx-auto text-green-500 mb-3"/><h2 className="text-lg font-semibold text-neutral-900 mb-1">{t('suggest.success_title')}</h2><p className="text-sm text-neutral-500">{t('suggest.success_desc')}</p><button onClick={()=>setOk(false)} className="mt-4 px-5 py-2.5 text-sm font-medium text-blue-600 hover:text-blue-700 transition-colors">{t('suggest.another')}</button></motion.div>):(<><div><label className="text-sm font-medium text-neutral-700 mb-1.5 block">{t('suggest.content')}</label><textarea value={c} onChange={e=>setC(e.target.value)} placeholder={t('suggest.placeholder')} rows={5} maxLength={1000} className="w-full px-3 py-2.5 text-sm border border-neutral-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-neutral-200 focus:border-neutral-300 transition-all resize-none"/><div className="text-xs text-neutral-400 mt-1 text-right">{c.length}/1000</div></div><div><label className="text-sm font-medium text-neutral-700 mb-1.5 block">{t('suggest.contact')}</label><input type="text" value={ct} onChange={e=>setCt(e.target.value)} placeholder={t('suggest.contact_placeholder')} className="w-full px-3 py-2.5 text-sm border border-neutral-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-neutral-200 focus:border-neutral-300 transition-all"/></div>{e&&<div className="text-sm text-red-600 bg-red-50 rounded-xl px-3 py-2.5">{e}</div>}<button onClick={hs} disabled={l} className="w-full py-3 bg-neutral-900 text-white text-sm font-semibold rounded-xl hover:bg-neutral-800 disabled:opacity-50 disabled:cursor-not-allowed transition-all flex items-center justify-center gap-2"><Send size={16}/>{l?t('suggest.submitting'):t('suggest.submit')}</button></>)}</div></AnimatedSection></div>);
}
