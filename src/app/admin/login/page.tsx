'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';import { motion } from 'framer-motion';import { Shield, Lock } from 'lucide-react';
export default function AdminLoginPage() { const router = useRouter(); const [p,setP] = useState(''); const [e,setE] = useState(''); const [l,setL] = useState(false);
const hl=async()=>{setE('');setL(true);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(r.ok)router.push('/admin/dashboard');else setE('密码错误')}catch{setE('网络错误')}setL(false)};
return(<div className="min-h-[calc(100vh-3.5rem)] flex items-center justify-center px-4"><motion.div initial={{opacity:0,y:16}} animate={{opacity:1,y:0}} transition={{duration:0.4,ease:[0.22,1,0.36,1]}} className="w-full max-w-sm"><div className="text-center mb-8"><div className="w-12 h-12 rounded-xl bg-neutral-100 flex items-center justify-center mx-auto mb-3"><Shield size={24} className="text-neutral-700"/></div><h1 className="text-xl font-bold text-neutral-900">管理后台</h1><p className="text-sm text-neutral-500 mt-1">输入管理员密码</p></div><div className="bg-white rounded-2xl border border-neutral-200/60 p-5 space-y-4"><input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="管理员密码" onKeyDown={e=>e.key==='Enter'&&hl()} className="w-full px-3 py-2.5 text-sm border border-neutral-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-neutral-200 focus:border-neutral-300 transition-all"/>{e&&<div className="text-sm text-red-600 bg-red-50 rounded-xl px-3 py-2.5">{e}</div>}<button onClick={hl} disabled={l} className="w-full py-3 bg-neutral-900 text-white text-sm font-semibold rounded-xl hover:bg-neutral-800 disabled:opacity-50 disabled:cursor-not-allowed transition-all flex items-center justify-center gap-2"><Lock size={16}/>{l?'登录中...':'登录'}</button></div></motion.div></div>);
}
