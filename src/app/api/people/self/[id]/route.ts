import { NextRequest, NextResponse } from 'next/server'; import { getDb } from '@/lib/db'; import bcrypt from 'bcryptjs'; import { verifyToken } from '@/lib/auth';
export async function PUT(r: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const token = r.headers.get('x-edit-token'); if(!token) return NextResponse.json({error:'未授权'},{status:401}); const pl = verifyToken(token); if(!pl||pl.id!==id) return NextResponse.json({error:'token无效或已过期'},{status:401}); const { name, class_name, photo_url, bio, password } = await r.json(); const db = getDb(); if(!db.prepare('SELECT id FROM people WHERE id=?').get(id)) return NextResponse.json({error:'人物不存在'},{status:404}); const ups = ["updated_at=datetime('now')"]; const vals: (string|null)[] = []; if(name!==undefined){ups.push('name=?');vals.push(name);} if(class_name!==undefined){ups.push('class_name=?');vals.push(class_name);} if(photo_url!==undefined){ups.push('photo_url=?');vals.push(photo_url);} if(bio!==undefined){ups.push('bio=?');vals.push(bio);} if(password){vals.push(await bcrypt.hash(password,10));ups.push('password_hash=?');} vals.push(id); db.prepare(`UPDATE people SET ${ups.join(',')} WHERE id=?`).run(...vals); return NextResponse.json(db.prepare('SELECT * FROM people WHERE id=?').get(id)); }
export async function DELETE(r: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const token = r.headers.get('x-edit-token'); if(!token) return NextResponse.json({error:'未授权'},{status:401}); const pl = verifyToken(token); if(!pl||pl.id!==id) return NextResponse.json({error:'token无效或已过期'},{status:401}); getDb().prepare('DELETE FROM people WHERE id=?').run(id); return NextResponse.json({success:true}); }
