'use client';import { useState } from 'react';export default function ImageWithFallback({ src, alt, className = '' }: { src: string; alt: string; className?: string }) { const [err, setErr] = useState(false); if (!src || err) return (<div className={`bg-neutral-100 flex items-center justify-center ${className}`}><div className="w-16 h-16 rounded-full bg-neutral-200 flex items-center justify-center"><span className="text-2xl text-neutral-400 font-medium">{alt.charAt(0)}</span></div></div>); return (<img src={src} alt={alt} className={className} onError={() => setErr(true)} />); }