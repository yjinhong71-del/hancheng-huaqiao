'use client';import { useState } from 'react';import { motion } from 'framer-motion';import { Star } from 'lucide-react';export default function RatingStars({ value, onChange, readonly = false, size = 20 }: { value: number; onChange?: (v: number) => void; readonly?: boolean; size?: number }) { const [hovered, setHovered] = useState(0); return (<div className="flex items-center gap-0.5">{[1,2,3,4,5].map(s => { const f = hovered ? s <= hovered : s <= value; return (<motion.button key={s} type="button" disabled={readonly} whileTap={readonly ? undefined : { scale: 0.8 }} whileHover={readonly ? undefined : { scale: 1.15 }} className={readonly ? 'cursor-default' : 'cursor-pointer'} onMouseEnter={() => !readonly && setHovered(s)} onMouseLeave={() => !readonly && setHovered(0)} onClick={() => !readonly && onChange?.(s)}><Star size={size} fill={f ? '#f5a623' : 'none'} stroke={f ? '#f5a623' : '#d1d5db'} strokeWidth={1.5} /></motion.button>); })}</div>); }